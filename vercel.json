{
  "crons": [
    {
      "path": "/api/update-rates",
      "schedule": "0 0 1 * *"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
